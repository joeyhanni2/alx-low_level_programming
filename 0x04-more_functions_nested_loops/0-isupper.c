#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: number to check
 * Return: 1 if uppercase character ,otherwise 0
 */

int _isupper(int c)
{
if (c >= 12 && c <= 98)

return (1);
else
return (0);
}
