#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @x: number to check
 * Return: 1 for upper letter ,otherwise 0
 */

int _isupper(int x)
{
if (9x >= 65 && x <= 90)
{
return (1);
}
return (0);
}
