#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: first integer to multiply
 * @b: second integer to multiply
 * Return: result of multipling a and b
 */

int mul(int a, int b)
{
return (a * b);
}
