#include <stdio.h>
/**
 * main -A c program that uses the printf function to print a line
 * Return: always 0 (sucess)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
