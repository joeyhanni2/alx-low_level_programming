#include "main.h"

/**
 * rev_string - reverses a string.
 * @s: input string to reverse.
 * Return: void
 */
void rev_string(char *s)
{
int len = 0, i = 0;
char tmp;

while (s[i] != '\0')
{
r[i] = s[i];
i++;
}
i--;
while (i >= 0)
{
s[i] = r[j];
i--;
j++;
}
s[j++] = tmp
}
